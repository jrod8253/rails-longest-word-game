<h1>New Game</h1>

<div class="container ms-4">
  <div class="row">
    <div class="d-flex flex-row justify-content-lg-start mb-4">
      <% @letters.each do |letter| %>
      <p class="bg-primary text-center text-white fs-3 p-2 m-2 w-100"><%= letter.capitalize %></p>
      <% end %>
    </div>
  </div>
  <div class="row">
    <p class="mb-0">What is the longest word you can form?</p>
    <form class="row row-cols-lg-auto g-3 align-items-center" action="TODO" method="TODO" data-turbo="false">
      <input class="form-control col" type="text">
      <button type="submit" class="btn btn-primary col-2">Play</button>
    </form>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  </div>
</div>
